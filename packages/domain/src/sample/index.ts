// @skinote/domain/sample — 견본 매장(목록 값 · 운영 규칙 · 견본 하루 · 명세 · 직원 예시). 체험판, 시험, 서버의 견본 매장 만들기가 쓴다.
// 운영 코드의 규칙은 이 폴더를 가져오지 않는다(견본 값은 매장 자료로만 들어간다).
export * from './registry.ts';
export * from './seed.ts';
export * from './spec.ts';
export * from './import.ts';
